---
phase: 4
plan: 1
wave: 1
---

# Plan 4.1: Data Ingestion (Vectorize O*NET)

## Objective
Run the vectorization script to process the O*NET dataset and populate the local ChromaDB. This is the foundational prerequisite for the AI pipeline.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- ai-service/data/onet_occupations.csv
- ai-service/data/vectorize_onet.py
- ai-service/data/verify_db.py

## Tasks

<task type="auto">
  <name>Populate ChromaDB</name>
  <files>
    - ai-service/data/vectorize_onet.py
    - ai-service/data/verify_db.py
  </files>
  <action>
    - Execute `vectorize_onet.py` from within the ai-service virtual environment to embed the CSV data using MiniLM.
    - If errors occur (e.g. data formatting or unencoded chars), fix them and re-run.
    - Run `verify_db.py` to ensure documents were stored correctly with metadata.
  </action>
  <verify>python -m data.verify_db</verify>
  <done>verify_db.py outputs success showing > 0 vectors stored in db_storage.</done>
</task>

## Success Criteria
- [ ] ChromaDB local directory `db_storage` is populated with `chroma.pkl` or similar persistent files.
- [ ] `verify_db.py` runs cleanly and confirms vector count matches the CSV rows.
