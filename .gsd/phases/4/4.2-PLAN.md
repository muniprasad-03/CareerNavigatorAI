---
phase: 4
plan: 2
wave: 2
---

# Plan 4.2: Inference Pipeline Validation

## Objective
Verify that the `inference.py` script correctly processes the loaded DB embeddings, accurately computes Cosine Similarity and RVI, handles the COURSE_CATALOG logic, and generates fallback SHAP/Ollama narratives without crashing.

## Context
- .gsd/SPEC.md
- ai-service/inference.py
- ai-service/main.py

## Tasks

<task type="auto">
  <name>Test Inference Pipeline Locally</name>
  <files>
    - ai-service/inference.py
    - ai-service/main.py
  </files>
  <action>
    - Verify that `COURSE_CATALOG` semantic matching is working as expected when testing the endpoints.
    - Make a curl or requests call to the local FastAPI `/match` endpoint with sample RIASEC and narrative JSON payload.
    - Ensure Ollama fallback (local Llama-3) is triggered and handles response elegantly.
    - Check the response JSON format to ensure it conforms to frontend expectations (top_careers array, title, score, rvi, shap_values, justification, next_step).
  </action>
  <verify>curl -X POST http://127.0.0.1:8000/match -H "Content-Type: application/json" -d '{"riasec_scores": {"R": 10, "I": 20, "A": 5, "S": 2, "E": 1, "C": 15}, "narrative": "I like to analyze data and build systems.", "north_star": ""}'</verify>
  <done>The API endpoint returns a 200 OK with the properly formatted JSON response containing SHAP values and a valid narrative justification.</done>
</task>

## Success Criteria
- [ ] The `/match` endpoint operates without 500 errors.
- [ ] The JSON response structure matches frontend component requirements perfectly.
- [ ] The `next_step` course selection dynamically changes based on the user's top RIASEC trait.
